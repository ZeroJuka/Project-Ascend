You are an expert mobile engineer tasked to fully build the {NAME TO BE DEFINED - USE "ASCEND" FOR THE TIME BEING} React Native + Expo application from scratch. This should be a multi-platform application capable of managing one's finances. The app should be an Expo app using Typescript and React-Native for the best responsiveness for every platform, mainly Mobile (Android and iOS).

This is an inovative application that views to bring various functionalities to make the User Experience the best possible, users wil have access to a conversational AI (Using the GEMINI 2.5 Pro API) as well as a voice transcription Service using OPENAI's Whisper via Hugging Face, the API keys were already set up, when using secrets in the code, add them to the  file, THERE SHOULD BE NO SECRETS IN THE CODE. This is done so the user is capable to not only consult their own metrics, spendings and overall economy aspect via Voice or Text but also should be able to Register new entries to their finances using those means, the idea is so the user would be able to enter the app, say "I bought a package of candy for 100$" and the app should be able to actually create that row.

The databse used is the Supabase Service, Auth and sql structure, the Auth component was already set up, but any other objects were not setup. If there is a need to execute any command to create new objects add these commands in order to the file  which will be manually run after the app is setup. The SUPABASE api key as other secrets should all be included ONLY in the  file.

Navigation should be done via the bottom navigation panel that should contain a larger button in the middle to represent the AI button. Touching the button should trigger navigation to the CHAT page and Holding the AI button should trigger the Whisper transcription for hearing the user and transcribing the text.

The user should be able to:
- Use Supabase Auth for sign in and sign up also protected by CAPTCHA.
- View a Home page with his main financial Metrics and page navigation buttons.
- Have access to a "HOME", "TRANSACTIONS", "GOALS and BILLS", "CHAT" and "DASHBOARD" pages.
- Be able to register Transactions manually as well as view his/hers current transactions in the TRANSACTIONS page using any of the default categories or creating their own, the categories should have custom colors and when registering a new category the user should be able to select a custom color as well for better visualization of the Transactions.
- Be able to set up Financial Goals to spend "at least" or "less than" a certain amount of money for a selected category or multiple of them, as well as set up Time Limited or Permanent bills like "Spend more than 200$ every month in bitcoin" or "Spending Less Than 100$ in clothing shopping and the cinema this week" in the GOAL page, as such goals could be "one time" or "recurring after a set amount of time" depending on the goals, there should be a progress bar applied to each goal so the user can see how they are faring towards each goal.
- Be able to Chat with Chat with Gemini about his Finances in the CHAT page, for that you should build a prompt to make a RAG for this Gemini Agent, this agent should be able to do SELECTs in the supabase object to deliver insights about his finances, CREATE transactions and goals automatically via this chat and DELETE Transactions or Goals when asked to FOR THAT USER, always have in mind the Security of other User data and to protect customer data every supabase object should be protected with RLS.
- Be able to chat with the AI via Voice using Whisper in every page by holding down the AI button, after sending a message, GEMINI should interpret it and define when the user is asking for a transaction or goal to be registered after such there should be a popup on which the user should confirm the informations of their transaction or goal, when asking for anything else they should be directed to the CHAT page and his message should be sent automatically to GEMINI. To make this transition better for the user, the chat bubbles should have a blue gradient for the user messages, a yellow gradient for the "whisper" messages and a grey message for the GEMINI default CHAT messages.

The UI for the app should be modern, responsive and very beautiful, one of the most important pre-requisites of this app is to promote the best User Experience Possible, keep in mind that this app should be very convenient for daily user, with easy navigation and visualization. Deliver a production-ready app with the same screens, flows, and data behavior. I got an inspiration from this image, for my UI.
Structure the files so they are optimized, there is no redundant code and is clean.

After coding, review if every requisite and functionality has been well implemented, make a brief explanation of everything I have to do manually, make sure there are no secret keys in the code and they are all in the .env file.
  